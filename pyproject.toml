[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wal-store"
version = "0.1.0"
description = "Write-ahead log key value store with crash recovery"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["wal_store"]
